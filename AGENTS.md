# EUM Frontend 개인 Codex 규칙

## 프로젝트 맥락

- 이 프로젝트는 팀 프로젝트다. 개인용 Codex 설정은 로컬에만 두고 커밋하지 않는다.
- Expo Router 기반 React Native 프론트엔드 프로젝트로 작업한다.
- 패키지 매니저는 `pnpm`을 사용한다.
- 새 구조를 만들기 전에 기존 앱 구조, 컴포넌트, 스타일, 라우트 패턴을 우선 확인한다.

## Git 안전 규칙

- 코드 변경 전 현재 브랜치와 `git status`를 확인한다.
- 명시적으로 요청받지 않는 한 commit, push, reset, rebase, 파일 삭제를 하지 않는다.
- 작업에 꼭 필요하지 않다면 `README.md`, `package.json`, `.gitignore`, 네이티브 프로젝트 설정 같은 공유 설정 파일을 수정하지 않는다.
- 개인용 변경은 `AGENTS.override.md`나 `.agents/`처럼 Git에서 무시되는 로컬 파일에만 둔다.

## 프론트엔드 작업 방식

- UI를 변경하기 전에 주변 화면과 컴포넌트를 먼저 확인한다.
- 반드시, 페이지 구성 시 재사용 가능한 컴포넌트가 존재하는지 먼저 확인 후 작업을 수행할 것
- React Native 컴포넌트는 타입을 명확히 하고 역할을 작게 유지한다.
- Expo Router의 파일 기반 라우팅 규칙을 따른다.
- 리디자인 요청이 없다면 기존 색상과 간격 패턴을 우선 사용한다.
- 앱에 보이는 문구를 추가할 때는 한국어를 우선 사용한다.
- 개발 단계에서 직접 수정할 때 편하게 하기 위해서 기능마다 주석을 첨부해서 읽기 편하게 할 것.
- 스타일은 native wind는 거의 사용하지 않고 css style sheet를 주로 사용할 것.
- 개발 단계에서 기능 및 플로우 확인을 위해서 index.tsx를 참고하여 개발한 기능의 플로우를 확인할 수 있게 제공할 것.

## 검증

- 코드 변경 후 가능하면 `pnpm lint`를 실행한다.
- 추후 TypeScript 검증 스크립트가 추가되면 repo에 있는 typecheck 명령을 사용한다.
- 실행하지 못한 검증이 있으면 명확히 알린다.

## 소통 방식

- 기본적으로 한국어로 답변한다.
- 파일을 수정하기 전에는 무엇을 왜 바꿀지 짧게 설명한다.
- 팀에 영향을 줄 수 있는 애매한 변경은 공유 파일을 건드리기 전에 먼저 물어본다.

## 홈페이지(home.tsx / (tabs)/index.tsx) 구현 스펙

### 페이지 구조

홈페이지는 ScrollView 내에 다음 섹션들이 순서대로 배치된다:

1. **헤더**: "환영합니다 [닉네임]님!" + 알림 아이콘 (우측)
2. **탭 네비게이션**: "좋" / "동호회" (선택된 탭에 핑크 언더라인)
3. **오늘의 이상형 추천**: 섹션 제목 + 실시간 카운트다운 (HH:MM:SS)
4. **프로필 카드**: 
   - 이미지 슬라이더 (여러 장 + 현재 페이지 점 표시)
   - 이름·나이 + 하트 아이콘
   - 위치 + 핀 아이콘
   - 자기소개 텍스트
   - "별로예요" / "마음이들어요" 버튼
5. **내 프로필을 본 인물들**: 가로 스크롤 리스트 (프로필 이미지)
6. **음성 입력 플로팅 버튼**: 우측 하단 고정, 핑크 원형 마이크 아이콘

### 컴포넌트 구조

- 헤더: fontSize 16-18, fontWeight 600
- 탭 버튼: 터치 영역 44x44px 이상, 활성 탭에 핑크 언더라인 (높이 3-4px)
- 섹션 제목: fontSize 16, fontWeight 600, marginBottom 12
- 프로필 카드: 백색 배경, borderRadius 16, 전체 너비에 padding 좌우 12
- 이미지 높이: 약 400-420px
- 슬라이더 점: 직경 8px, 활성 #FF1B4D, 비활성 #E0E0E0
- 카운트다운: fontSize 12, color #A0A0A0, 위치 오른쪽 상단
- 이름·나이: fontSize 18, fontWeight 700, color #202020
- 위치: fontSize 14, color #A0A0A0
- 자기소개: fontSize 14, color #202020, marginTop 8
- 버튼: 높이 44px, borderRadius 8
  - "별로예요": backgroundColor #E8E8E8, color #202020
  - "마음이들어요": backgroundColor #FF1B4D, color #FFFFFF
- 음성 입력 버튼 (FAB): 
  - 직경 60px, borderRadius 50% (원형)
  - backgroundColor #FF1B4D
  - 위치: **position fixed** (스크롤 시에도 화면에 고정)
    - 우측에서 20px, 하단 네비게이션 바로부터 20px 위
  - 마이크 아이콘 흰색
  - **스크롤 동작**: 
    - 아래로 스크롤 시 살짝 사라짐 (opacity/translateY 애니메이션)
    - 위로 스크롤 시 다시 나타남 (스크롤 방향 감지)

### 스타일 참고값

- 주요 색상: 핑크 #FF1B4D, 검정 #202020, 회색 #A0A0A0, 라이트 그레이 #E8E8E8
- 배경: 흰색 #FFFFFF
- 섹션 간 상단 패딩: 24px
- 카드 내 요소 간 간격: 12px

### 데이터 처리

- 사용자 닉네임은 임시로 목 데이터 사용
- 카운트다운은 실시간 업데이트 (useEffect + setInterval)
- 프로필 이미지는 Image 컴포넌트로 처리
- 슬라이더 상태는 ScrollView의 onScroll 또는 별도 state로 관리

### 사용자 상호작용

- "마음이들어요": heart 탭으로 이동 (누적 카운트 증가)
- "별로예요": 다음 프로필로 카드 전환
- **이미지 좌우 스와이프**: 
  - 좌우 스와이프로 다음/이전 사진 전환
  - Page Indicator(점)가 함께 이동하여 현재 사진 위치 표시
  - 예: 3번째 사진 보면 점 3개 중 3번째가 활성화(#FF1B4D)
- 프로필 카드 터치: 프로필 상세 페이지로 네비게이션
- "내 프로필을 본 인물들" 클릭: 프로필 상세 페이지로 네비게이션
- 음성 입력 마이크 버튼: 음성 입력 모달/페이지로 이동 (ideal-recording.tsx)
